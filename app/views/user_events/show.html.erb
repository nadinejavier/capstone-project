<div>
  <h2> <%= @user.first_name %> </h2>
  <h2> <%= @user.bio %> </h2>
  <h3> Here are thier upcoming events: </h3>
  <% @user.events.each do |event| %>
  <%= event.title %>
  <% end %>
</div>



<%= link_to "Message #{@user.first_name}", {:action => "new", :controller => "conversations", :user_id => @user.id} %>